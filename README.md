Готово, Камиль — ниже **полностью оформленный README.md**, без комментариев, без подсветок, без пояснений — просто копируешь в GitHub.

---

# 📌 **ГОТОВЫЙ README.md (полностью готов к копированию)**

---

```md
# Deposit AI — сервис анализа депозитных предложений и расчёта доходности

Веб-приложение на **Spring Boot + Thymeleaf**, позволяющее пользователям сравнивать депозитные предложения, рассчитывать доходность и управлять банками через админ-панель. Система включает регистрацию, авторизацию, роли пользователей и встроенную базу данных H2.

---

## 🚀 Основной функционал
- Регистрация и вход пользователей (Spring Security)
- Просмотр всех депозитных предложений
- Расчёт доходности выбранного депозита
- Автоматический поиск банка с максимальной доходностью
- Админ-панель для добавления и редактирования депозитов
- Работа с H2 Database
- UI на Bootstrap 5 + кастомные стили

---

## 🛠 Используемые технологии

| Технология | Описание |
|-----------|----------|
| **Java 21** | логика приложения |
| **Spring Boot 3** | каркас backend-части |
| **Spring MVC** | маршрутизация и контроллеры |
| **Spring Security 6** | роли, авторизация, защита |
| **Spring Data JPA** | работа с хранилищем данных |
| **H2 Database** | встроенная БД |
| **Thymeleaf** | HTML-шаблоны |
| **Bootstrap 5** | интерфейс |
| **Maven** | сборка проекта |

---

## 📂 Архитектура проекта

```

src/main/java/com/coursework/deposit
│
├── config                # настройки Spring Security
├── controller            # DepositController, AdminController, AuthController
├── model                 # сущности UserEntity, DepositEntity, Role, DTO
├── repository            # UserRepository, DepositRepository
├── service               # CustomUserDetailsService, DepositService
└── Application.java      # точка входа

````

### (Рисунок 1 — Общая архитектура проекта)
Вставить изображение схемы слоёв Spring Boot:
`images/architecture.png`

---

## 🔐 Система авторизации

В приложении реализованы роли:

- `ROLE_ADMIN`
- `ROLE_USER`

Пользовательские данные загружаются через `CustomUserDetailsService`:

```java
return new User(
        user.getUsername(),
        user.getPassword(),
        user.isEnabled(),
        true,
        true,
        true,
        List.of(new SimpleGrantedAuthority("ROLE_" + user.getRole().name()))
);
````

Эта строка реализует **полиморфизм**, адаптируя пользовательскую сущность под интерфейс `UserDetails`.

---

## 🗄 База данных (H2)

H2-консоль доступна по адресу:

```
http://localhost:8080/h2-console
```

JDBC URL используется файл:

```
jdbc:h2:file:./data/depositsdb
```

### (Рисунок 2 — Интерфейс H2 Database)

`images/h2.png`

---

## 🖥 Интерфейс пользователя

### Главная страница

Отображает:

* таблицу депозитов,
* форму добавления депозита (если ADMIN),
* форму расчёта доходности,
* кнопку «Найти лучший депозит».

### (Рисунок 3 — Главная страница Index)

`images/index.png`

---

### Страница входа

/ login — стандартная форма авторизации

### (Рисунок 4 — Страница входа)

`images/login.png`

---

### Админ-панель

Раздел `/admin/deposits` с таблицей всех депозитов и формой добавления.

### (Рисунок 5 — Админ-панель)

`images/admin.png`

---

## 📊 Логика расчёта доходности

Метод сервиса:

```java
public double calculateIncome(Long depositId, double amount) {
    DepositEntity dep = repository.findById(depositId).orElseThrow();
    return amount * (dep.getInterestRate()/100) * (dep.getPeriodMonths()/12.0);
}
```

Также реализован поиск лучшего банка:

```java
public DepositEntity findBest(double amount) {
    return getAll().stream()
            .max(Comparator.comparing(dep -> calculateIncome(dep.getId(), amount)))
            .orElse(null);
}
```

---

## ▶ Запуск проекта

### 1️⃣ Склонировать

```
git clone https://github.com/USERNAME/deposit-ai.git
cd deposit-ai
```

### 2️⃣ Запустить

```
mvn spring-boot:run
```

### 3️⃣ Открыть сайт

```
http://localhost:8080/
```

---

## 🔑 Данные по умолчанию

| Логин | Пароль | Роль  |
| ----- | ------ | ----- |
| admin | admin  | ADMIN |
| user  | user   | USER  |

---

## 📌 Возможности дальнейшего расширения

* Перенос на PostgreSQL
* JWT-авторизация
* Добавление API для мобильных клиентов
* Графики доходности с Plotly
* Сортировка и фильтрация депозитов

---

## 👨‍💻 Автор

**Дутбаев Камиль (ТИИ-34)**
Евразийский национальный университет им. Л.Н. Гумилёва
Курсовая работа по дисциплине **ООП на Java**

---

## 📄 Лицензия

MIT License

```



Сказать «сделай картинки»?
```
